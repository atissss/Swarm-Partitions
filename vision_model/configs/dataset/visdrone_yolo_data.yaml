# Ultralytics-format dataset descriptor, consumed by Detector.train()/validate()
# (Ultralytics' YOLO.train(data=...) expects exactly this shape). Class index
# order here MUST match models.yolo_detector.DEFAULT_CLASS_MAP.
path: data
train: VisDrone2019-DET-train/images
val: VisDrone2019-DET-val/images
test: VisDrone2019-DET-test-dev/images

names:
  0: car
  1: person
